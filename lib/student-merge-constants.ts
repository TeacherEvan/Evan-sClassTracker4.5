/**
 * Shared constants for student merge and sync system
 */

export const STUDENT_MERGE_CONSTANTS = {
    /**
     * Minimum length for merge/delete reason
     */
    MIN_REASON_LENGTH: 10,
    
    /**
     * Maximum length for merge/delete reason
     */
    MAX_REASON_LENGTH: 500,
} as const;
